# Re-Auditoría SEO — ProductosVirales.com.ar

**Fecha:** 13 de abril de 2026 (segunda pasada)  
**Dominio:** productosvirales.com.ar  

---

## Resumen ejecutivo

Se implementaron parcialmente los fixes del primer audit, pero **hay 3 archivos truncados que rompen el sitio**. Hasta que se reparen, el sitio no compila ni despliega. La situación es peor que antes del intento de fix porque ahora hay código roto en producción (o próximo a producción).

**Prioridad absoluta:** Reparar los 3 archivos truncados antes de cualquier otra cosa.

---

## Progreso vs Audit #1: Qué se arregló y qué no

| Fix del Audit #1 | Estado | Detalle |
|---|---|---|
| 1. Crear `robots.ts` | ✅ Hecho | Archivo existe con allow, disallow /api/, sitemap URL |
| 2. Viewport export en layout.tsx | ✅ Hecho | `Viewport` con width, initialScale, maximumScale |
| 3. Twitter card en layout.tsx | ✅ Hecho | `summary_large_image` configurado |
| 4. Icons en layout.tsx | ⚠️ Parcial | Referencia a `/favicon.ico` y `/apple-touch-icon.png` pero **los archivos no existen en /public** |
| 5. Canonical en layout.tsx | ✅ Hecho | Canonical apunta al dominio |
| 6. Metadata en home (page.tsx) | ⚠️ ROTO | Metadata fue agregada pero **el archivo está truncado** — la función del componente fue cortada |
| 7. Organization + WebSite schema en home | ❌ No hecho | Imposible porque el archivo está truncado |
| 8. Trending metadata | ✅ Hecho | Canonical, OG, twitter — todo correcto |
| 9. Categorías generateStaticParams | ✅ Hecho | Pre-renders categorías no especiales |
| 10. Categorías canonical/OG/twitter | ✅ Hecho | Metadata completa |
| 11. Categorías BreadcrumbList | ✅ Hecho | JSON-LD correcto |
| 12. Productos generateStaticParams | ✅ Hecho | Top 50 productos no deprioritized |
| 13. Productos canonical/twitter | ✅ Hecho | Metadata completa con images |
| 14. Productos BreadcrumbList | ❌ No hecho | **Y el archivo está truncado** — falta resto del JSX |
| 15. Remover unoptimized de ProductCard | ✅ Hecho | Prop eliminada |
| 16. Remover unoptimized de ProductDetail | ✅ Hecho | Prop eliminada |
| 17. Guías index OG/twitter | ✅ Hecho | Metadata completa |
| 18. Guías [slug] BreadcrumbList | ❌ No hecho | Sin schema BreadcrumbList |
| 19. Guías [slug] OG images | ❌ No hecho | Sin images en openGraph ni twitter |
| 20. next.config.ts headers | ⚠️ ROTO | Headers function **truncada** — return sin valor |

---

## PROBLEMAS CRÍTICOS (sitio no compila)

### 1. `src/app/page.tsx` — TRUNCADO (línea 32)

El archivo termina en `export default` sin cuerpo de función. Se perdió todo el componente Home:

```
// Línea 32 actual:
export default
// FIN DEL ARCHIVO — faltan ~18 líneas del componente
```

**Contenido perdido:** El `async function Home()` completo con HeroBanner, HomeFeed, TrendingBar, WhatsAppCTA, ProductGrid, PriceAlert, HomeFAQ.

### 2. `src/app/producto/[id]/page.tsx` — TRUNCADO (línea 122)

El archivo se corta en medio del JSX return. Faltan ~6 líneas:

```
// Línea 122 actual:
        />
// FIN DEL ARCHIVO — faltan cierre de faqLd, ProductDetail, ProductGrids y divs
```

**Contenido perdido:** El cierre del condicional faqLd, `<ProductDetail>`, los dos `<ProductGrid>` de productos relacionados, y los divs de cierre.

### 3. `next.config.ts` — TRUNCADO (línea 17)

La función `headers()` no tiene valor de retorno:

```
// Líneas 16-17 actuales:
  async headers() {
    return
// FIN DEL ARCHIVO — falta el array de headers y los cierres
```

**Contenido perdido:** El array completo de headers (X-Content-Type-Options, X-Frame-Options, Referrer-Policy) y los cierres de objeto/export.

---

## PROBLEMAS PENDIENTES (no críticos, post-reparación)

### Severidad ALTA

| # | Problema | Archivo | Detalle |
|---|---|---|---|
| 1 | **Favicon/icons no existen** | `/public/` | layout.tsx referencia `favicon.ico` y `apple-touch-icon.png` pero no existen en /public. Solo hay SVGs genéricos de Next.js |
| 2 | **Organization schema faltante** | `src/app/page.tsx` | Después de reparar el archivo, agregar JSON-LD Organization |
| 3 | **WebSite schema faltante** | `src/app/page.tsx` | Después de reparar, agregar JSON-LD WebSite con SearchAction |
| 4 | **BreadcrumbList faltante en productos** | `src/app/producto/[id]/page.tsx` | Después de reparar, agregar BreadcrumbList JSON-LD |

### Severidad MEDIA

| # | Problema | Archivo | Detalle |
|---|---|---|---|
| 5 | **BreadcrumbList faltante en guías** | `src/app/guias/[slug]/page.tsx` | Sin JSON-LD BreadcrumbList (Home > Guías > [Guía]) |
| 6 | **OG images faltante en guías** | `src/app/guias/[slug]/page.tsx` | openGraph y twitter no incluyen `images` |
| 7 | **SVGs genéricos en /public** | `/public/` | file.svg, globe.svg, next.svg, vercel.svg, window.svg son restos del boilerplate de Next.js — deberían removerse |

---

## Estado completo de cada archivo

### ✅ Archivos OK (no requieren cambios)

| Archivo | Estado |
|---|---|
| `src/app/robots.ts` | Perfecto — allow, disallow /api/, sitemap |
| `src/app/layout.tsx` | Perfecto — viewport, twitter, icons refs, canonical |
| `src/app/trending/page.tsx` | Perfecto — canonical, OG, twitter |
| `src/app/categoria/[slug]/page.tsx` | Perfecto — generateStaticParams, canonical, OG, twitter, BreadcrumbList |
| `src/app/guias/page.tsx` | Perfecto — canonical, OG, twitter |
| `src/app/sitemap.ts` | Perfecto — dinámico, bien priorizado |
| `src/components/products/ProductCard.tsx` | Perfecto — sin unoptimized |
| `src/components/products/ProductDetail.tsx` | Perfecto — sin unoptimized |

### ❌ Archivos que necesitan reparación/cambios

| Archivo | Problema | Acción |
|---|---|---|
| `src/app/page.tsx` | **TRUNCADO** — falta componente Home | Restaurar función completa + agregar schemas |
| `src/app/producto/[id]/page.tsx` | **TRUNCADO** — falta fin del JSX | Restaurar JSX completo + agregar BreadcrumbList |
| `next.config.ts` | **TRUNCADO** — headers sin return value | Completar función headers |
| `src/app/guias/[slug]/page.tsx` | Falta BreadcrumbList + OG images | Agregar schema + images en metadata |
| `/public/` | Faltan favicon.ico y apple-touch-icon.png | Crear/generar los iconos |

---

## Checklist de indexación (sin cambios desde Audit #1)

El sitio sigue sin estar indexado en Google. Una vez reparados los archivos:

1. ✅ `robots.ts` ya existe — bien
2. ⬜ Registrar sitemap en Google Search Console
3. ⬜ Verificar que el sitio es accesible públicamente (DNS, SSL, no redirect loops)
4. ⬜ Hacer `Request Indexing` en GSC para las páginas principales
5. ⬜ Verificar que el build pasa limpio antes de deployar
